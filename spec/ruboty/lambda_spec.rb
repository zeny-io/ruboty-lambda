require "spec_helper"

describe Ruboty::Lambda do
  it "has a version number" do
    expect(Ruboty::Lambda::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
